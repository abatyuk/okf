---
type: Skill
title: okf:infer-ontology skill
description: Derives an ontology from an existing bundle by analyzing observed types, common fields, and reference patterns, then commits the proposal via okf ontology.
trigger: reverse-engineer an ontology from a bundle
uses:
- /commands/list
- /commands/stats
- /commands/ontology-add
---
# okf:infer-ontology skill

Derives an ontology from an existing bundle by analyzing observed types, common fields, and reference patterns, then commits the proposal via okf ontology. The reverse of authoring types by hand.
